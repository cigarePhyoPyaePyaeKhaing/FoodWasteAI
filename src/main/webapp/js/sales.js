/**
 * FoodWaste AI - Sales Controller
 */
const Sales = {
  init() {
    console.log('Sales controller initialized');
  }
};

document.addEventListener('DOMContentLoaded', () => {
  Sales.init();
});
