# Seven-day prediction details delivery report

1. **Previous limitations.** Commit 494ab2d already provided item cards and expandable daily forecasts; the older aggregate-only presentation described in the request was no longer the main modal. Missing details were generated time, evaluated counts, per-item estimated potential loss, current business reason, recommended action, redistribution status, and a neutral zero-waste label.
2. **New structure.** The existing 640px rounded modal is retained: forecast summary, individual evaluated-item cards, expandable daily records, and a business-friendly footer with Close. No site redesign or extra page was added.
3. **Item fields.** Name, category, unit, current stock, expiry date, current days remaining, expected daily demand, current risk and score, projected surplus, seven-day waste, estimated potential loss, redistribution status, reason, and recommended action are visible. Historical confirmed waste rate and explicit current-risk aliases are additionally supplied by the API. Food IDs remain in the API without cluttering the cards.
4. **Daily fields.** Existing date, opening stock, expected demand, days to expiry, future risk/score, predicted sales, predicted waste, closing stock, and bilingual reason remain expandable under each item. The label now explicitly says Daily Forecast Risk. No days are invented after depletion.
5. **API changes.** The existing read-only report adds `generatedAt` (UTC instant for the fresh computation), `evaluatedItemCount`, and `forecastDayCount`. `forecastItems` gains `historicalWasteRate`, `currentRiskLevel`, `currentRiskScore`, `estimatedPotentialLoss`, `reason`, `reasonMy`, `recommendedActionMy`, and bilingual redistribution labels. Existing keys and endpoints remain compatible; current `items` are not repurposed.
6. **DTO changes.** The existing report/detail maps in PredictionService are extended. No new DTO class or PrologAssessment policy changes were needed. Current facts and bilingual wording come from the existing backend current assessment. Estimated loss is computed server-side as seven-day waste × item price, rounded to two currency decimals; absent price is null, displayed as unavailable rather than invented money.
7. **Reconciliation.** The original canonical daily forecast and KPI algorithm are unchanged. Item waste still sums its daily waste; summary maps sum those quantities per unit. The UI only formats backend quantities and monetary values. Java tests verify these relations and loss reconciliation; browser tests compare the detail summary against the Dashboard KPI.
8. **Zero-prediction items.** All active evaluated items remain visible, including operationally relevant HIGH-risk items with zero forecast waste. An explicit neutral `0 Predicted Waste` badge prevents a risk badge from implying positive predicted waste. Redistribution status appears separately, including No Surplus.
9. **Messages.** The exact requested title is used in English and Myanmar. Generated time is displayed in Asia/Yangon using the shared timestamp formatter. No-forecast, no-inventory, valid-zero, API-error, and partial-detail states remain distinct. Missing quantities/prices display an em dash. Estimated money is labeled Estimated Potential Loss, never Saved.
10. **Technical wording.** The previous empty implementation footer becomes “Forecast based on current inventory, demand, expiry, and waste history,” with Myanmar translation. Technical implementation strings in dynamic reasons/actions are suppressed with an unavailable-detail message. Regression coverage injects technical terms to verify they are not rendered.
11. **Responsive/theming.** Item headings wrap with existing risk badges, the neutral badge uses theme variables, and facts retain the responsive grid/stack layout. Long item names and Myanmar text wrap; content scrolls vertically inside the modal. Desktop, tablet, mobile, English, Myanmar, light, and dark combinations are exercised.
12. **Files modified.** PredictionService.java; TranslationService.java; dashboard.html; responsive.css; dashboard.js; i18n/en.js and mm.js; MetricForecastConsistencyTest.java; SevenDayForecastRegressionTest.java; ui-consistency.test.cjs; this report. No Prolog rules, risk classifier, stock-history logic, completed/pending aggregation, or forecast quantity algorithm changed.
13. **Tests.** Two Java regressions were added for metadata, current facts, translated text, monetary reconciliation, missing price, and current-versus-future risk separation. Existing reconciliation/risk/depletion tests remain. The real-engine check identified and now covers the missing Myanmar translation of the existing remaining-stock recommendation. The old title assertion was updated to the user's requested title. Browser coverage adds counts/time, zero badge, No Surplus, loss/recommendation fields, technical-text suppression, and twelve viewport/language/theme screenshot combinations.
14. **Maven.** Baseline: 326 tests, zero failures/errors/skips; clean package BUILD SUCCESS, 01:02 min. Final results are recorded below after verification.
15. **Real Prolog/frontend.** Real SWI-Prolog tests and frontend checks are run separately; results below. Browser tests use intercepted local fixtures, with no production writes. A generated forecast fixture comes from Java tests, rather than manually invented forecast quantities.
16. **Commit.** Requested message: `feat(forecast): add item-level seven-day prediction details`. The actual commit hash is reported in the completion message.
17. **Push.** Target is `origin/main`. The requested desktop checkout still has unrelated history and was preserved. Work was completed in the verified `C:\FoodWasteAI\risk-fix` clone. Actual push and any live read-only deployment checks are reported in the completion message.

Limits: forecast quantities remain estimates under the unchanged demand/no-replenishment model. Display rounding is one decimal per quantity, while API values retain precision. Potential loss is an estimate at the current inventory price, not a confirmed loss or saving. The API computes fresh forecasts on GET, so generated time describes that computation rather than the timestamp of an older saved evaluation.
## Final verification

- `mvn clean test package`: **328 tests, 0 failures, 0 errors, 0 skipped; BUILD SUCCESS; 57.679 s**.
- Real SWI-Prolog suite: **57 tests, 0 failures, 0 errors, 0 skipped; BUILD SUCCESS; 01:36 min**.
- All JavaScript syntax checks and the existing Dashboard risk regression passed.
- Browser regression passed all seven pages and the enhanced modal: desktop/tablet/mobile × English/Myanmar × light/dark, with no JavaScript errors. Screenshots were inspected, including mobile Myanmar dark mode; the footer Close button remains unwrapped.
- Forecast quantity/risk logic, redistribution rules, completed/pending metrics, pizza transaction semantics, and historical records were preserved.
